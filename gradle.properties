org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=35

# AGP 9.0 Migration - Built-in Kotlin is enabled by default
# If issues arise, can temporarily opt-out with: android.builtInKotlin=false

# AGP 9.0 Migration - New DSL is enabled by default
# If third-party plugins have issues, can temporarily opt-out with: android.newDsl=false
